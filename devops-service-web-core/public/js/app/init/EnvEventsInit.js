define([

//  'App'
//  'events/Environment'

], function() {

  'use strict';

  return {
    initEvents : function() {

      //        var envHandler = new EnvironmentEventHandler();

      /*        App.vent.on('manageEnvUsers', function(data) {*/
      //EnvironmentEventHandler.manageUsers(data);
      //});
      //App.vent.on('deployEnv', function(data) {
      //if(confirm('Sure?')) {
      //EnvironmentEventHandler.deployEnv(data);
      //}
      //});
      //App.vent.on('viewServers', function(data) {
      //EnvironmentEventHandler.viewServers(data);
      //});
      //App.vent.on('env:addEnv', function(data) {
      //EnvironmentEventHandler.addEnv(data);
      /*});*/
    }
  };

});
